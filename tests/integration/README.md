# Integration tests placeholder

Use Playwright here after the frontend and API are running together.
Suggested checks:
- Home page loads at http://localhost:3000
- Links tab renders correctly
- Users and posts data loads from the API
